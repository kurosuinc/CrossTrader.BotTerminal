<v:CustomWindow
    x:Class="CrossTrader.ViewerExample.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:CrossTrader.ViewerExample.ViewModels"
    xmlns:v="clr-namespace:CrossTrader.ViewerExample.Views"
    mc:Ignorable="d"
    Title="LoginWindow"
    SizeToContent="WidthAndHeight">
    <v:CustomWindow.DataContext>
        <vm:LoginWindowViewModel />
    </v:CustomWindow.DataContext>
    <DockPanel>
        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            DockPanel.Dock="Bottom">
            <Button
                Command="{Binding ShowExchangesCommand}">Exchanges</Button>
            <Button
                Command="{Binding ShowInstrumentsCommand}">Instruments</Button>
        </StackPanel>
        <StackPanel
            Orientation="Horizontal"
            VerticalAlignment="Center">
            <Label>Host:</Label>
            <TextBox
                Text="{Binding Host}" />
            <Label>Port:</Label>
            <TextBox
                Text="{Binding Port}" />
        </StackPanel>
    </DockPanel>
</v:CustomWindow>
